# Copy this file to .env (do not commit .env)

# Gmail credentials (App Password required: enable 2FA and generate in Google Account)
SMTP_EMAIL=
SMTP_APP_PASSWORD=

# Who receives the daily summary
OWNER_EMAIL=

# Display name for the sender
FROM_NAME=3N Licenças

# Comma-separated day thresholds for reminders (exact days before expiry)
# Example: 30,15,5
DAYS_THRESHOLDS=30,15,5

