# Example environment variables for DadApp local sync
# Copy to DadApp.env or .env and fill in values.



# Render API endpoint that receives the client list
RENDER_API_URL=

# Render API key/token (if your endpoint requires Authorization header)
RENDER_API_KEY=

